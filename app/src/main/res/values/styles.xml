<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/bootstrap_gray_light</item>
        <item name="colorPrimaryDark">@color/bootstrap_gray</item>
        <item name="colorAccent">@color/bootstrap_brand_secondary_border</item>
    </style>

</resources>
